before_install:
  - sudo apt-get --assume-yes install libeXosip2

language: python
python:
  - "3.4"
  - "3.5"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: cd src && python -m unittest
